<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>MusicDb</title>

  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootswatch/3.0.2/cosmo/bootstrap.min.css">
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.2/css/font-awesome.min.css">

  <style>
    /* Only necessary if you are using navbar-fixed-top */
    body {
      padding-top: 50px;
    }
  </style>

  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.2/js/bootstrap.min.js"></script>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <%= render 'layouts/navbar' %>
  <div class="container-fluid">
    <div class="row" id="wrapper">
      <% if user_signed_in? %>
        <%= render 'layouts/sidebar' %>
      <% else %>
        <%= render 'layouts/sidebar_empty' %>
      <% end %>
      <div class="col-lg-12" id="page-content">
        <% if notice.present? %>
          <div class="alert alert-dismissable alert-success" id="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            <%= notice %>
          </div>
        <% end %>
        <%= yield %>
      </div>
      <footer class="col-lg-12 text-center">
        <br><br>Like this app?<br>
        Check us out on<br><a href="http://twitter.com/improv42" target="_blank"><%= fa_icon 'twitter lg' %></a><br><br>
      </footer>
    </div>
  </div>
</body>
</html>
