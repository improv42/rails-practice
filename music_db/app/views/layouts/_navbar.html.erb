 <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header" id="logo">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to fa_icon( 'headphones lg', text: 'Music'), root_url, :class => "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right navbar-collapse" >
        <% if user_signed_in? %>
          <li class="visible-xs"><%= link_to "Songs", songs_url %></li>
          <li class="visible-xs"><%= link_to "Artists", artists_url %></li>
          <li class="visible-xs"><%= link_to "Genres", genres_url %></li>
          <li class="visible-xs"><hr></li>
        <% end %>
        <% if user_signed_in? %>
          <li><%= link_to fa_icon('user', text: 'Account'), edit_user_registration_path %></li>
          <li><%= link_to fa_icon('sign-out', text: 'Logout'), destroy_user_session_path, :method => :delete %></li>
        <% else %>
          <li><%= link_to fa_icon('sign-in', text: 'Login'), new_user_session_path %></li>
          <li><%= link_to fa_icon('user-plus', text: 'Sign Up'), new_user_registration_path %></li>
        <% end %>
        <li><%= link_to 'About', pages_about_path %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </nav>