<h3><%= @owner.name %><br><small><%= @owner.phone %></small><hr></h3>

<ul>
  <% @owner.dogs.each do |dog| %>
    <li><%= dog.name %></li>
  <% end %>
</ul>


<%= link_to 'Edit', edit_owner_path(@owner), class: "btn btn-info btn-sm" %>
<%= link_to 'Back', owners_path, class: "btn btn-info btn-sm" %>
